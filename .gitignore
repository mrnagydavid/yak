settings.local.json
node_modules
.pnpm-store
dist
.vite
*.local
dev-dist/
Swedish Kelly-20260602134631.apkg
# Large/derived seed build artifacts are ignored; the LLM cleanup decisions and generated
# example sentences are kept so the seed stays regenerable (re-run fetch → join → flag → apply).
data/intermediate/*
!data/intermediate/decisions/
!data/intermediate/examples/
!data/intermediate/sense-decisions.json
!data/intermediate/translation-decisions.json
