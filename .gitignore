settings.local.json
node_modules
.pnpm-store
dist
.vite
*.local
dev-dist/
Swedish Kelly-20260602134631.apkg
# Regenerable seed build artifacts: fetch/join caches (kelly, kaikki dump, wik, ipa), the emitted
# batching inputs (ambiguous/multi-translation/flagged), and all batch dirs. Disposable — reproducible
# by re-running fetch → join → apply → stale → batch. The COMMITTED inputs (base + the per-layer
# ledgers + decisions + stale) live under data/seed/sv/ and are intentionally NOT ignored.
data/scratch/
# Legacy pre-refactor working dir; its contents were relocated into data/seed/sv/ (tracked) or are
# disposable. Ignored until the directory is removed.
data/intermediate/
